---
"@gantryland/task": patch
"@gantryland/task-cache": patch
"@gantryland/task-combinators": patch
---

- Add package-level runtime metadata and publish safeguards (`engines.node` and `prepack` build checks) across the three core packages.
- Keep release and publish ergonomics clear with root scripts for safer monorepo publishing.
- Improve OSS repository operations with CI and governance documentation updates.
